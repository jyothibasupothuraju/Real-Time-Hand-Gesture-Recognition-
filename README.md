# Real-Time-Hand-Gesture-Recognition-
A computer vision project that detects hand landmarks in real time using MediaPipe and classifies them into alphabet gestures using a trained ML model — built with OpenCV and Python.
